# Batch 1 — Implementation Plan

**Basis:** `docs/design/portal-v2-spec.md` (frozen v1) · `prantivo-mockups-batch1.html` (approved 2026-07-28)
**Scope granted:** token updates · shell/navigation changes · page-level class updates · the three new components
**Scope denied:** routes, backend behaviour, APIs, configService, schema, SQL
**Sessions:** 5 (D1–D5), one issue each
**Commit to:** `docs/specs/portal-v2-batch1.md`

---

## 0. What is verified and what is not

Everything below is derived from excerpts quoted in `2026-07-frontend.md` and from `project_knowledge_search` hits on `public/portal/tokens.css` and `public/portal/shell.js`. **I do not have the working tree.** Every path, line number and current token value is therefore *indicative*, and Phase 0 of each session reconciles it against the real repo before changing anything. The backend-engineer standing rule applies: spec says an indicative name, check the real tree, use the real path, proceed.

Three things are asserted with high confidence because the audit measured them directly:

- `public/portal/tokens.css` is 21,070 bytes, declares 43 custom properties, and is loaded by **all 14** portal pages.
- `public/portal/` is 14 `.html`, 15 `.js`, 15 `.css`, plus `fonts/` (1 `.css` + 6 `.woff2`).
- `npm test` is `node --test --require ./tests/_support/testEnv.js tests/**/*.test.js` and returns **830 / 142 suites / fail 0**. It executes nothing in `web/`.

### 0.1 Correction to the frozen spec

The spec says *no custom property is renamed*. That is wrong for two properties and the error would have been silent and expensive.

The existing ramp uses `--teal-600: #0d6b63` and `--teal-700: #0f5f59` as **darker steps below** the base `--teal: #0f766e`. The v2 ramp uses the standard 50–900 convention where **`--teal-700` *is* the base** at `#0f766e`. Same two names, two incompatible conventions. Redefining `--teal-700` in place would silently repaint every existing consumer — `.nav__item--active` among them — with no compile error and no failing test.

**Resolution (D1):** the two existing names are re-pointed to intent-named properties, `--teal-hover` and `--teal-press`, and every consumer inside `public/portal/` is updated mechanically. Blast radius is exactly `public/portal/`, because each surface declares its own `:root` — `demo/shared.css` and `demo/styles.css` are unaffected by a change in `tokens.css`. Phase 0 proves that with a grep before the edit, not after.

Everything else in the spec's "nothing is renamed" claim holds: `--radius`, `--radius-sm`, `--radius-xs`, `--shadow`, `--shadow-sm`, `--teal-050`, `--accent`, `--s-1`…`--s-7` all survive as aliases.

### 0.2 Two claims from earlier turns that need correcting

**F-F008 is decided here, not closed here.** The brand decision is made — teal wins as one ramp, `#0f766e` is the light-ground step, `#14b8a6` is the ink-ground step. But closing the finding requires editing `web/app/globals.css`, which is outside all four granted buckets and outside `public/portal/`. That is a separate ~1h session. Batch 1 delivers the portal half and leaves the finding open.

**The command palette has no mockup.** The spec names three new components — truth strip, Verbatim panel, command palette — and Batch 1 drew the first two. The `⌘K` hint appears in the top bar on every artboard; the panel itself does not. It is a well-understood pattern with low design risk, so D2 implements it to spec and its DoD screenshot serves as the mockup after the fact. If you would rather approve a drawing first, cut it from D2 and it moves to Batch 2 — the hint stays either way, because a visible affordance for a feature that exists is not a placeholder.

---

## 1. Change surface

Files touched at all, across all five sessions:

| Path | Sessions | Nature |
|---|---|---|
| `public/portal/tokens.css` | D1 D2 D3 D4 D5 | `:root` values, then component rules |
| `public/portal/fonts/fonts.css` | D1 | **regenerated by its existing script, never hand-edited** |
| `public/portal/fonts/*.woff2` | D1 | 4 files added (Noto Sans Latin 400/500/600/700) |
| `public/portal/shell.js` | D2 D3 | nav render, check copy map, truth-strip mount |
| `public/portal/*.html` × 14 | D2 D3 D4 D5 | sidebar markup, class swaps, panel mount point |
| `public/portal/*.css` × 14 page files | D5 | class swaps only |
| `public/portal/*.js` × page files | D3 D5 | empty/loading/error render strings only |
| **new** `public/portal/verbatim.js` + `.css` | D4 | the panel |
| **new** `public/portal/cmdk.js` | D2 | the palette |
| `docs/design/brand-values.md` | D1 | **new** — the canonical token table |
| `tests/design/tokenDrift.test.js` | D1 | **new** — asserts the table against the stylesheets |

Files that must not appear in any diff: anything under `src/`, `server.js`, `infra/`, `schema.sql`, `tests/` except the one new file above, `web/`, `public/admin/`, `public/demo/`.

---

## 2. Element → file map

Every visual element on the approved artboards, mapped to where it is implemented. Grouped by session.

### 2.1 Tokens — D1

| Mockup element | Property | Current | New | Consumers |
|---|---|---|---|---|
| Teal ramp base | `--teal-700` | `#0f5f59` | **`#0f766e`** | see §0.1 — collision |
| Primary hover | `--teal-800` | — *(was `--teal-600 #0d6b63`)* | `#115e59` | `.btn--pri:hover` |
| Primary press | `--teal-900` | — *(was `--teal-700 #0f5f59`)* | `#134e4a` | `.btn--pri:active` |
| Legacy dark steps | `--teal-hover` `--teal-press` | new names | `#0d6b63` `#0f5f59` | transitional; consumers migrated in D1 |
| Active nav tint | `--teal-50` | `--teal-050 #f0fdfa` | same value, canonical name; `--teal-050` kept as alias | `.nav__item--active` |
| Badge fill | `--teal-100` | `#ccfbf1` | unchanged | `.badge` |
| Chart / hover fill | `--teal-200` | — | `#99f6e4` | added, unused in Batch 1 |
| Ink-ground accent | `--teal-500` | — | `#14b8a6` | Verbatim panel, D4 |
| Ink-ground hover | `--teal-400` | — | `#2dd4bf` | Verbatim panel, D4 |
| Ink ground | `--field` | — | `#0c1420` | panel, Test, Sign-in pane |
| Ink raised | `--field-2` | — | `#141c2a` | bubbles |
| Ink hairline | `--field-line` | — | `rgba(255,255,255,.10)` | panel dividers |
| Ink text | `--field-ink` | — | `#e8edf2` | panel body |
| Ink meta | `--field-muted` | — | `#94a3b8` | gloss, labels |
| Border darkening on hover | `--line-3` | — | `#cbd5e1` | `.btn--sec:hover`, `.in:hover` |
| Semantic 200 steps | `--green-200` `--red-200` | — | `#bbf7d0` `#fecaca` | badges, banners (`--amber-200` already exists) |
| Semantic 500 steps | `--green-500` `--amber-500` `--red-500` | — | `#22c55e` `#f59e0b` `#dc2626` | live dot, input error border |
| Semantic 700 aliases | `--amber-700` `--red-700` | `--amber` `--red` | alias to existing values | unchanged rendering |
| Card radius | `--r-md` ← `--radius` | `14px` | **`10px`** | every `.card` |
| Control radius | `--r-sm` ← `--radius-sm` | `10px` | **`6px`** | `.btn`, inputs, `.nav__item` |
| Badge radius | `--r-xs` ← `--radius-xs` | `8px` | **`4px`** | `.badge`, checkbox |
| Modal radius | `--r-lg` | — | `14px` | modals, sheets, panel |
| Pill radius | `--r-full` | — | `999px` | ring, toggle, avatar |
| Menu shadow | `--shadow-sm` | `0 1px 2px …06` | unchanged | dropdowns |
| Float shadow | `--shadow-md` ← `--shadow` | two-layer `.04/.06` | `0 2px 4px rgba(15,23,42,.05), 0 8px 20px rgba(15,23,42,.08)` | toasts, mobile save bar |
| Modal shadow | `--shadow-lg` | — | `0 4px 8px rgba(15,23,42,.06), 0 20px 48px rgba(15,23,42,.14)` | modals, drawer |
| **Card shadow removed** | — | `.card { box-shadow: var(--shadow) }` | **declaration deleted**; card is `1px solid var(--line)` | largest single visual change |
| Spacing extension | `--s-8` `--s-9` | — | `64px` `96px` | empty states, sign-in |
| Body face | `--sans` | `'Inter', system-ui, …` | **`'Noto Sans', system-ui, …`** | whole portal |
| Type scale | `--t-display` … `--t-micro` | — | 9 properties per spec §2.2 | added; applied in D5 |
| Motion | `--dur-1`…`--dur-4`, 3 easings | — | per spec §2.7 | added; applied D2–D5 |
| Panel width | `--panel-w` | — | `360px` | D4 |

**Font pipeline.** `fonts.css` is generated and marked *Do not edit by hand*. Phase 0 locates the generator (likely `scripts/`), adds Noto Sans Latin 400/500/600/700 to its input, and re-runs it. Four `.woff2` land in `public/portal/fonts/`, `unicode-range`-scoped to Latin + Latin-Ext, `font-display: swap`, same-origin. **No CDN, no Google Fonts link, no build step.** The mockup file loads from Google purely for portability and is not the reference for this.

**Deliberate divergence to record.** `--sans` now differs from `demo/shared.css` and `demo/styles.css`. Record it in `brand-values.md` at the point of divergence, exactly as `tokens.css:19-20` already records why `--bg` differs from the demo's. A recorded divergence is a decision; an unrecorded one is F-F008.

### 2.2 Shell and navigation — D2

| Mockup element | File | Change |
|---|---|---|
| Grouped sidebar (4 group headers) | `shell.js` nav render + `tokens.css` | new `.grp` rule; nav array gains a `group` key |
| Nav order | `shell.js` | Home · Your clinic (5) · What it knows (2) · How it behaves (2) · Check (2) |
| Reserved `TODAY` slot | `shell.js` | group defined, empty, not rendered when it has no items |
| Active-item 2px left bar | `tokens.css` `.nav__item--active::before` | new pseudo-element; survives greyscale where a tint alone does not |
| `Soon` badge on unbuilt items | existing `tokens.css` pattern | **preserve as built**, restyle only |
| Brand mark 30→28px, `--r-sm` | `.brand__mark` | value change |
| Clinic identity footer | `shell.js` + `.side__foot` | injected, not added to each `.html` — one copy cannot drift. Ships clinic name + **role** (no email exists; see spec §3.0) |
| Top bar 56px | `.top` | height + padding |
| Lifecycle control (4 states) | `shell.js` + `.lc` rules | reads existing lifecycle state already in shell's payload |
| `⌘K` hint | `.kbd` | static |
| Command palette | **new** `cmdk.js` | fuzzy over the **12 navigation destinations**; no network |
| Page header + hairline | `.page-head` | `--t-h1`, bottom border, `--s-6` margin |
| Mobile drawer | existing `.side` transform | preserve mechanism, restyle |
| `is-embedded` wizard mode | existing rules | **must survive unchanged** — verify the wizard iframe still renders |

**No new data.** The lifecycle control renders from state `shell.js` already receives. If it does not, the control ships in its `Not live` form and the session records that as a finding rather than adding a route.

### 2.3 Truth strip and state sweep — D3

| Mockup element | File | Change |
|---|---|---|
| Truth strip component | `tokens.css` `.ts` + `shell.js` mount | new; renders between `.top` and `.page-head` |
| Condition: legacy prompt | `shell.js` | reads the **existing** `tenant.legacy_prompt` check already in the validation payload |
| Condition: not live / paused / partial | `shell.js` | reads existing lifecycle state |
| `shell.js:109` label rewrite | `shell.js` | `'Using the latest instruction format'` → states the condition instead of denying it |
| Check removed from ring list | `shell.js` | it is not a task the owner can complete; putting it in a checklist implies otherwise |
| `material: false` at `:109` | `shell.js` | **stays false.** Do not lift it. Lifting it without the strip in place gives the owner an unactionable red check. |
| Grouped checks (`Needed to go live` / `Handled by Prantivo`) | `shell.js` + `.ck__g` | new grouping over the existing copy map |
| Ring restyle + `--dur-4` draw | `.ring` rules | `sessionStorage` guard, once per session |
| Empty states × 10 hooks | `doctors.js:385,419` `faqs.js:266,301` `history.js:251,256` `hours.js:166` `pricing.js:199` `test.js:26,68` | string + markup only |
| `test.js:68` rewrite | `test.js` | `'No saved config yet'` → *Save your clinic details first — the test uses your real settings.* + link |
| Skeletons | page `.js` files | replace any spinner where the layout is known |
| Page-level error block | `.emp` variant | new rule, reused |
| Global reduced-motion block | `tokens.css` | replaces the narrow `:287-289` block; model is `web/app/globals.css:117-134` |

**This is the one session where a test edit is permitted.** If a test asserts the old `shell.js:109` string, updating that assertion is in scope. Any *other* test change is a STOP.

**F-F001's backend cause is untouched.** `validationService.js:293-299` returns `warn(...)`. It stays returning `warn(...)`. The strip reads that warn; it does not need it promoted to `fail`.

### 2.4 Verbatim panel — D4

| Mockup element | File | Change |
|---|---|---|
| Panel shell, docked / rail / sheet | **new** `verbatim.css` + `verbatim.js` | mounted on 8 editing pages + Test |
| Preview content | `verbatim.js` | consumes the **existing** prompt-preview endpoint |
| Language selector | `verbatim.js` | lists enabled languages from config already loaded on the page |
| Vernacular type + `lang` | `verbatim.css` | 19/34, `--te`/`--hi`; `lang` set per string |
| Mandatory English gloss | `verbatim.js` | never `aria-hidden` — it is the accessible content for a non-Telugu-reading owner |
| FACTS block | `verbatim.js` | derived from the page's already-loaded config section |
| Live warnings | `verbatim.js` | tappable, focuses the offending field |
| 600 ms debounce + top progress line | `verbatim.js` | previous value stays readable while regenerating |
| `Hear it` | `verbatim.js` | **ships disabled with its reason adjacent** |
| Collapse state | `localStorage` | the one permitted client-storage use |

**Hard dependency, resolved in Phase 0.** The panel needs a preview under `/portal/api/`. Portal-v1 §5.15 implies one exists. If Phase 0 finds it is operator-only, **the session does not add a route.** The panel ships reading the page's already-loaded config section and renders the FACTS block only, with the greeting bubble showing the raw stored greeting rather than the rendered composite. That is a degraded but honest panel, and the missing endpoint becomes a separate issue.

### 2.5 Component sweep and mobile — D5

| Mockup element | File | Change |
|---|---|---|
| Buttons — 5 variants × 6 states | `tokens.css` `.btn*` | 36/32/44px heights, `--r-sm`, weight 500 |
| Disabled + adjacent reason | 14 `.html` | markup — every disabled primary gains a reason sibling |
| Inputs — rest/hover/focus/error/read-only/disabled | `tokens.css` | 40px, 44px mobile; read-only visually distinct from disabled |
| `+91` and `₹` prefix segments | `tokens.css` `.pre` | new; no change to `normalizePhone` behaviour |
| Field error | existing `.field__error` `:412-425` | restyle only — the comment *"Errors name the fix (§4)"* stays |
| Toggle, segmented control | `tokens.css` | state word beside the switch, so state is not colour-only |
| Card | `.card` | **shadow deleted**, `1px solid var(--line)`, `--s-5` padding |
| Card footer states | `.card__foot` | clean / dirty / saving / saved · `v{N}` in `--mono` |
| Table | `.tb` rules | horizontal rules only — **no vertical rules, no zebra striping** |
| Tabular figures | `tokens.css` | `font-variant-numeric: tabular-nums` on every numeric cell class |
| Table → card below 768px | page `.css` files | per-table; **zero horizontal scroll at 320px** |
| Sticky mobile save bar | `tokens.css` + page `.js` | appears on dirty; safe-area inset respected |
| Badges | existing `.badge` | icon + word always |
| Toasts | existing toast | bottom-right desktop, **top-centre mobile** |
| Focus ring | `tokens.css:406`, `login.html:26` | keep the substitution, retune to 2px/2px offset |

---

## 3. Sessions

House format. One issue each. Runtime evidence closes.

### D1 · `feat(portal): v2 design tokens and self-hosted Noto Sans`

**Phase 0 — STOP conditions**
1. `grep -rn "teal-600\|teal-700" public/portal/` — enumerate every consumer. If any consumer lives outside `public/portal/`, **STOP** and report.
2. Locate the `fonts.css` generator script. If it cannot be found, **STOP** — do not hand-edit a file marked *Do not edit by hand*.
3. Confirm `tokens.css` is linked by all 14 pages. If any page declares its own `:root`, **STOP**.
4. Baseline `npm test` and record the raw line.
5. Capture before-screenshots of Home, Pricing, Receptionist at 1440 and 380.

**Hard constraints**
- `:root` and component *values* only. No new markup, no JS, no new class names.
- `fonts.css` regenerated, never hand-edited. No CDN, no Google Fonts link, no build step in `public/`.
- `public/demo/`, `public/admin/`, `web/`, `src/`, `tests/` (except the one new file) untouched.

**Scope**
Full teal ramp with the §0.1 collision resolved · `--field*` group · `--line-3` · semantic 200/500/700 steps · radius scale with legacy aliases · elevation scale with the card shadow deleted · `--s-8` `--s-9` · type scale · motion tokens · `--panel-w` · `--sans` → Noto Sans + 4 self-hosted faces · `docs/design/brand-values.md` · `tests/design/tokenDrift.test.js`.

**Out of scope**
Nav grouping · truth strip · panel · any `.html` edit · any page `.js` edit · `web/`'s `--accent`.

**Done when**
- `npm test` returns **`tests 831 / fail 0`** — 830 unchanged plus the one new drift test. Any other delta is a defect.
- Before/after screenshots at 1440 and 380 for the three pages.
- `grep -rn "Inter" public/portal/` returns nothing.
- The four `.woff2` are same-origin and `unicode-range`-scoped; DevTools Network shows zero external font requests.
- Telugu and Devanagari still render on Receptionist — the pipeline change must not disturb the existing six faces.

---

### D2 · `feat(portal): grouped navigation, top bar, lifecycle control, command palette`

**Phase 0 — STOP conditions**
1. D1 merged and on `main`.
2. Read the current nav render in `shell.js`. If nav is hardcoded per page rather than shared, **STOP** and report — grouping 14 copies is a different issue.
3. Confirm lifecycle state is present in the payload shell already receives. If not, the control ships in its `Not live` form; **do not add a route.**
4. Confirm `body.is-embedded` / `.app.is-embedded` behaviour and record how to verify the wizard iframe after the change.

**Hard constraints**
- No route added. No fetch added. The palette is client-side over static page titles.
- `is-embedded` must still hide sidebar, scrim and top bar, and still suppress `.page-head__title`.
- The `Soon` badge pattern is preserved, not reinvented.

**Scope**
Group headers and nav order · active-item left bar · sidebar identity footer · top bar geometry · lifecycle control (4 states) · `⌘K` hint · `cmdk.js` · page-header restyle · mobile drawer restyle.

**Out of scope**
Truth strip · Verbatim panel · card/table/input restyles · empty states.

**Done when**
- `npm test` returns **`tests 831 / fail 0`**, unchanged.
- Screenshots: the sidebar on all **12 navigation destinations** at 1440 (not 14 — `login.html` has no shell and `wizard.html` is not a destination); drawer open at 380; **the wizard at 380 with `is-embedded` active**, proving the iframe still renders correctly.
- `⌘K` opens, arrows navigate, Enter routes, Escape closes. Keyboard-only screenshot with a visible focus ring.
- Lifecycle control screenshotted in all four states against real tenant data.

**LANDED — `ae5e607`.** Closing suite **869 / 151 / fail 0**, unmoved from the D1 baseline. The `831` above was stale when written; the invariant that held was the *delta*, not the number. Four corrections this session made to the plan and spec, all recorded above and in spec §3.0 / §1.3 / §3.7:
1. **12 navigation destinations**, not 14 — see spec §3.0 for the full count breakdown.
2. **`knows.html` filed under CHECK**; it was missing from the spec's nav drawing entirely, and the sidebar is its only inbound link.
3. **`Documents` restored as an inert `Soon` row** — it has no page; `PORTAL-P6-S18` had removed it, and the v2 spec reverses that deliberately.
4. **Sidebar footer ships role, not email** — `/portal/api/me` returns `{ id, role }` and no address.

One process note worth carrying forward: **Issue 17's single-writer guard greps `scripts/` as well as `src/`.** A throwaway screenshot script that forced lifecycle states with a raw `UPDATE tenants SET status` failed the suite, correctly. Drive states through `lifecycleService.transition()` instead.

---

### D3 · `feat(portal): truth strip and empty/loading/error sweep`

**Phase 0 — STOP conditions**
1. D2 merged.
2. Read `validationService.js:293-299`. Confirm `tenant.legacy_prompt` arrives in the payload as a `warn`. **Do not modify that file.** If the check is absent from what the portal receives, **STOP** — the strip has no data and that is a backend issue, not this one.
3. `grep -rn "Using the latest instruction format" tests/` — if a test asserts the string, note it; updating that assertion is the only permitted test edit.
4. Enumerate the 10 empty-state hooks and record each current string.

**Hard constraints**
- `material: false` at `shell.js:109` **stays false.**
- `validationService` returns `warn(...)` and keeps returning `warn(...)`. No severity promotion.
- The four-layer error ladder is preserved. `'Something went wrong. Try again.'` remains the last-resort fallback after `data.error`. **Do not collapse the ladder.**
- The `receptionist.html:99` gap notice keeps its copy; it may be restyled to the standard inset and nothing more.
- The unknown-check default at `shell.js:111-117` stays.

**Scope**
Truth strip component + 4 conditions · `shell.js:109` label rewrite + removal from the ring list · check grouping · ring restyle and `--dur-4` draw with `sessionStorage` guard · 10 empty states · skeletons · page-level error block · global reduced-motion block.

**Out of scope**
Verbatim panel · buttons/inputs/tables · mobile table conversion.

**Done when**
- `npm test` returns `fail 0`. Count may move by at most the one permitted assertion update; state which.
- **Screenshot of the amber strip on a tenant with `tenants.ai_prompt` set.** This is the evidence that closes F-F001's portal half. Without it the session is not done.
- Screenshots of all 10 empty states, 4 skeletons, and the page-level error.
- Reduced-motion screenshot: ring at final value, live dot static, nothing left invisible.

---

### D4 · `feat(portal): verbatim preview panel`

**Phase 0 — STOP conditions**
1. **Resolve F-V001 before writing panel code.** Hard STOP gate for D4. Check order:
   1. Full Noto Sans face
   2. `unicode-range: U+20B9`
   3. Accept and document in `docs/design/brand-values.md`
2. D3 merged.
3. Locate the prompt-preview endpoint under `/portal/api/`. **If it is operator-only, do not add a route** — ship the degraded panel per §2.4 and file the endpoint as a separate issue. Report which path was taken before writing code.
4. Confirm the enabled-languages list is available client-side on each editing page.
5. **Before writing any code:** render the fixture Telugu strings and confirm zero tofu at 19px/600 on the target devices. The panel makes vernacular rendering the most prominent element in the product; if the faces are wrong this session is premature.
6. **Read `knows.html`, report exactly what it renders, and determine whether it is a subset of the Verbatim panel.** `knows.html` ("What it knows", `PORTAL-P5-S15`) answers the same question the panel answers — *what will the receptionist actually say?* — and D2 filed it under CHECK without deciding its future (spec §1.3). Report the finding, then choose:
   - **Retire it** if the panel is a strict superset — two surfaces answering one question is how they drift apart.
   - **Retain it as a linked advanced view** if it shows anything the panel does not (whole-config breadth versus per-page depth), and link the panel to it.

   **Report before deciding.** The sidebar is `knows.html`'s only inbound link, so deciding from assumption rather than from the page deletes a real surface.

**Hard constraints**
- No route added. No configService call. No write path of any kind — the panel is read-only.
- The English gloss is mandatory and never `aria-hidden`.
- `lang` on every vernacular string, following the `public/demo/app.js:105` pattern.
- `Hear it` ships **disabled with its reason adjacent.** Do not wire TTS.
- `localStorage` for collapse state only.

**Scope**
`verbatim.css` + `verbatim.js` · docked / rail / sheet forms · language selector · gloss · FACTS block · live warnings · 600 ms debounce with the top progress line · mount on 8 editing pages + Test · `aria-live="polite"`.

**Out of scope**
Everything else. This is the largest new surface in Batch 1 and it does not share a session.

**Done when**
- `npm test` returns `fail 0`, count unchanged.
- Screenshots at 1440 docked, 1024 rail, 380 sheet, on a **real tenant with Telugu enabled**.
- Telugu renders with correct conjuncts and matras at 19/34 — verified on a real Android device, not an emulator.
- Editing a fee updates the FACTS block within one debounce, with the previous value readable throughout.
- Keyboard: panel reachable by Tab, warnings focus their field, Escape collapses the sheet.

**LANDED — `08f2fa4`** (harness `8a36d22`, docs `1403631`). Closing suite
**869 / 151 / fail 0**, unmoved. Two Phase-0 rulings this session made, recorded
here because the plan asked for them and only `docs/os/state.md` carried the
answer until D5a backfilled it:

1. **`knows.html` is NOT a subset of the Verbatim panel. RETAINED, and its nav
   row stays.** This settles the question §2.4 and D2's correction 2 left open.
   It carries whole-config breadth in one view, and the built-in-protections
   card, which quotes verified guardrail instructions the panel never shows —
   the panel is per-page depth, `knows.html` is whole-clinic breadth. The panel
   links to it (*See all*). `knows.html` was not modified, and it is not retired
   in D5 either.
2. **No prompt-preview endpoint exists** — filed as F-V004 below, and the panel
   ships the degraded-but-honest form §2.4 authorised.

---

### D5 · `feat(portal): component sweep and mobile pass`

**Phase 0 — STOP conditions**
1. D4 merged.
2. Enumerate every `<table>` in `public/portal/` and record its intended card form **before** editing any of them.
3. `grep -rn "width:\|min-width:" public/portal/ | grep -v max-width` — record the baseline. It must be zero at ≥380px and must still be zero afterwards.
4. Confirm the `Save changes → Saving… → Saved` strings across all 10 writing pages and record them.

**Hard constraints**
- The save-state discipline is preserved **exactly**, including `fill(data.section)` refilling the server's returned value into the form. Restyle only.
- No `<table>` may keep horizontal scroll below 768px.
- Zero fixed widths ≥380px may be introduced. This is the audit's Preserve-As-Is #9.
- Toasts never carry validation errors.
- Focus indicators are never colour-only.

**Scope**
Buttons (5 × 6 states) · disabled-plus-reason markup · inputs (6 states) · prefix segments · toggle and segmented control · card shadow removal and footer states · table rules and tabular figures · table→card below 768px · sticky mobile save bar · badges · toasts · focus ring retune.

**Out of scope**
Anything not on the Batch 1 artboards.

**Done when**
- `npm test` returns `fail 0`, count unchanged.
- Screenshots of **all 14 pages at 1440 and 380**.
- Zero horizontal scroll at 320px on all 14. Recorded as a scripted check, not eyeballed.
- Every disabled control in the portal has a visible reason beside it.
- Keyboard tab-through of Pricing and Safety & handoff with the ring visible at every stop.

**D5 WAS SPLIT.** D5a (this commit) took the shared component layer plus the six
worklist items that had accumulated across D1–D4. **D5b still owes:** table rules
and tabular figures, table→card below 768px, the sticky mobile save bar, and the
320px sweep — with §3's Phase-0 items 2 and 3 (enumerate every `<table>`; record
the fixed-width baseline) still unspent, because D5a touched no table.

**D5a — LANDED.** Suite **869 / 151 / fail 0**, unmoved. Worklist: W1 (17
consumers resolved per site, both transitional tokens deleted), W2 (`.doc`,
`.faq` shadows gone), W3 (`.modal` → `--shadow-lg`; the drawer already had it),
W4 (`--amber-50`/`--green-50`/`--red-50` canonical, padded names aliased), W5
(not-live strip suppressed on Home), W6 (F-V004's contradiction — see §6).
Components: buttons 5×6, inputs 6 states with read-only visually distinct from
disabled, toggle and segmented carrying their state word, badges, card-footer
states, toasts, the portal's first global focus ring, and a visible reason beside
every disabled control.

Three things D5a found that the plan did not predict, all recorded at the point
of use:
1. **There was no portal-wide focus ring at all.** §2.5's row calls this a
   "retune" of `tokens.css:406` + `login.html:26`; both references were stale,
   and the real inventory was five component-local rules with every button, link,
   nav item and modal close falling through to the UA default. `H` was
   *adding* the ring, not retuning it — and `booking-rules.css:105` was stripping
   the outline and replacing it with a colour-only tint, which §2.11 forbids and
   which the second copy of the same toggle never did.
2. **`.btn--danger` was a solid red fill.** Now `--card` with a `--red-200`
   hairline and `--red-700` text, per §2.9: a filled red button is the most
   attractive target on screen at the moment the owner is about to do something
   irreversible.
3. **Ten page scripts each carried their own `toast()`, and all ten faded ERRORS
   out after 2.6s.** Now one implementation in `shell.js`: success clears itself
   at 4s with `role="status"`, an error persists with a Dismiss and
   `role="alert"`. An error toast that removes itself is one the owner can miss,
   and what they missed is that their change did not save.

**One deliberate deviation from spec §2.9, recorded rather than silently
resolved:** a busy button keeps `disabled`. The spec says busy is
"non-interactive but not disabled, so it keeps focus"; every save path in this
portal uses `disabled` as its ONLY double-submit guard, and this session's hard
constraint was that the save discipline behave identically. `aria-busy`, the
spinner and the held width all ship; `disabled` stays until a re-entry guard
exists to replace it. `Portal.setBusy()` is the single seam where that swap will
happen.

**The `v{N}` is not in `--mono`.** §2.9 asks for it. `Portal.savedMessage()`
returns `Saved · v13` as one string and ten page scripts write it with
`textContent`; wrapping the numeral needs those scripts to switch to `innerHTML`,
which is the save path. Tabular figures on the whole note buy the column
alignment the mono was for. Deferred to a session allowed to touch the save call.

---

## 4. Invariant guard

Any session whose diff touches one of these has left scope. Check before commit.

**From the audit's Preserve As-Is:**
1. `public/{demo,portal}/fonts/fonts.css` and the six existing `.woff2` — generated, `unicode-range`-scoped, self-hosted. D1 *adds* faces through the generator; it does not alter the six.
2. The `lang="te"` pattern at `public/demo/app.js:105`.
3. `tokens.css` as the single shared stylesheet loaded by all 14 pages.
4. The `receptionist.html:99` gap-notice copy.
5. `Save changes → Saving… → Saved`, including the server-value refill.
6. The four-layer error ladder.
7. The unknown-check default at `shell.js:111-117`.
8. Zero fixed widths ≥380px.

**From portal-v1 §3, unaffected but restated because a UI session must not weaken them:**
INV-1 tenant scoping · INV-2 configService as the only write path · INV-3 owners cannot skip checks or see collections · INV-4 acting user on every revision · INV-5 upload validation · INV-6 `normalizePhone`. No session in Batch 1 touches a route, so none of these can regress — but if a diff appears to touch one, that diff is wrong.

**Copy rules that survive:** product is the **AI Receptionist**; "AI Employee" and "AI Operating System" appear nowhere; `Submit` appears nowhere.

---

## 5. Verification protocol

**The test count is the signal.** Batch 1 changes no behaviour, so `npm test` should hold at 830 through D2, D4 and D5, move to 831 at D1 (the drift test), and move by at most one assertion at D3. **Any other movement means a session touched behaviour and must be stopped and explained.** That inverts the usual reading: here, a *changing* test count is the alarm.

**Per session, pasted into the issue:**
- the raw `tests N / fail 0` block
- the screenshots named in that session's DoD
- `git diff --stat` proving no file outside §1 was touched

**Batch acceptance, after D5.** Re-run the portal-v1 §11 criterion end to end on a fresh tenant: operator creates an owner account; the owner completes the wizard on a phone unaided in under 45 minutes; readiness reaches green; Go live; then edit a treatment price and confirm the Test page quotes the new price on the next message. The redesign is only successful if that run is *faster* than it was before. If it is slower, the sticky save bar or the table→card conversions are wrong and D5 reopens.

---

## 6. Outside this scope

Three items, named so they are not silently absorbed:

1. **F-F008's `web/` half** — `web/app/globals.css` `--accent`. One session, ~1h, after Batch 1. Until then the finding stays open.
2. **A native Telugu review** of the greeting and price fixture strings. Blocking for D4's Phase 0. Not an engineering task.
3. **`README.md:141`** still reads *"Build the AI Operating System for businesses."* Not a surface, so it breaches nothing — but it is the first thing a visitor to the repository reads, and it contradicts the naming rule this whole redesign enforces. One line, any session.
4. **F-V001 — CLOSED at D4 Phase 0, on resolution path 1.** The full Noto Sans
   family *does* carry U+20B9 — but in its `devanagari` subset file, not the
   `latin` one the portal ships. Enumerating every subset Google serves confirms
   it: of `Noto Sans`'s eight subsets only `devanagari` declares U+20B9, and
   `Noto Sans Telugu` declares it in none. Shipping that whole 120 KB subset for
   one glyph would also have shadowed the Devanagari family for Hindi, so the
   glyph was taken alone through Google's `text=` subsetter: **four
   weight-distinct static faces of ~830 bytes** (400/500/600/700, 3.2 KB total),
   family `Noto Sans`, `unicode-range: U+20b9` as Google itself declares it,
   generated by a new `rupee` entry in `scripts/demo/fetch_fonts.js`. The ten
   pre-existing faces regenerated byte-identical and `public/demo/` was untouched.
   Asserted, not eyeballed, via `CSS.getPlatformFontsForNode`: at 600 19px/34px
   the string `₹1,50,000` renders as `NotoSans-SemiBold` ×1 glyph (the sign) plus
   `Noto-Sans-SemiBold` ×8 (the digits), both `isCustomFont: true` — one typeface
   at one weight. The control, the same string with Noto removed from the stack,
   renders entirely in `SegoeUI-Semibold`. Corroborated independently by the
   browser fetching `noto-rupee-{400,500,600}.woff2` (a unicode-range-scoped face
   is only downloaded when a glyph in that range is actually needed) and by
   `document.fonts` reporting those three faces `loaded`.
   ⚠️ The two reported `familyName` strings differ (`Noto Sans SemiBold` vs
   `Noto Sans`) because Chrome echoes each woff2's internal name record and
   Google's `text=` subsetter writes a different one from its `unicode-range`
   subsetter. They are the same typeface; compare `postScriptName`, not
   `familyName`, if this is ever re-checked.
5. **F-V002 — Variable font duplication.** Noto Sans is a variable font. Multiple `@font-face` rules currently serve the same underlying font file. Touches `public/demo/`. Not a Batch 1 task — own session after Batch 1. Intended fix:
   - one `@font-face` per family
   - `font-weight: 400 700`
   - generated through `scripts/demo/fetch_fonts.js`

   ⚠️ **EXCLUSION — read before consolidating anything.** The four
   `noto-rupee-{400,500,600,700}.woff2` faces that F-V001 added are **NOT**
   duplication and must survive this session unchanged. They are `text=`-subsetted
   **static** faces of ~830 bytes each carrying exactly U+20B9, declared under
   family `Noto Sans` with `unicode-range: U+20b9`. Folding them into one variable
   `@font-face` at `font-weight: 400 700` would either drop the rupee glyph or
   pull in the 120 KB Devanagari subset that F-V001 deliberately avoided — and
   the failure is silent: `₹` falls back to system-ui and renders in a different
   typeface from the digits beside it, which is the exact defect F-V001 closed.
   Consolidate the **ten `unicode-range`-subsetted faces** only. Re-assert with
   `CSS.getPlatformFontsForNode` against a control, as F-V001 did; comparing
   `postScriptName`, not `familyName` (see item 4's trap).
6. **F-V004 — no owner-facing rendered-composite preview exists.** Found in D4
   Phase 0; the panel ships around it. §2.4 assumed "the existing prompt-preview
   endpoint" and portal-v1 §5.15 implies one — there is none. All 36 routes in
   `src/portal/routes.js` were walked. The nearest is
   `GET /portal/api/knowledge-summary`, which is owner-scoped and returns the
   assembled FACTS but *deliberately* not the rendered prompt: its own comment
   reads "their returned prompt TEXT never crosses this route", and owners are
   never shown prompt syntax, tool schemas or guardrail wording there. The
   operator-side preview in the admin panel is not owner-reachable.
   **Consequence:** the Verbatim panel's greeting bubble shows the clinic's
   *stored* greeting, not the composite the renderer will actually emit, so
   precedence effects — a legacy `tenants.ai_prompt` shadowing the config
   (A-007), or renderer gating that drops a section — are invisible in the
   preview. The portal-wide truth strip (D3) is what currently warns about the
   first of those; nothing covers the second. Fixing this needs a new owner-scoped
   route over `renderSystemPrompt`, plus a decision about how much of the
   composite an owner should see. Out of scope for Batch 1 — D4's hard constraint
   was no new route.

   **The CONTRADICTION half is CLOSED (D5a/W6); the missing endpoint is still
   open.** The panel labelled itself *Live preview* beside a pulsing teal dot
   while, on a legacy clinic, the truth strip 40px above it said those settings
   were not reaching the receptionist. Two components, one screen, opposite
   claims — and the confident one was wrong. When the legacy condition holds the
   header now reads **`Saved settings`** and drops the live dot, sourced from the
   same `run.checks` `tenant.legacy_prompt` severity `shadow-notice.js` reads,
   through the same shared readiness promise; no new fetch. Asserted in
   `scripts/portal/shootD5a.js` and photographed with the amber strip in the same
   frame (`shots/d5a-w6-legacy-pricing.png`), against a clean-tenant control that
   still reads *Live preview* with its dot.
   **Still open:** there is no rendered-composite endpoint, so the greeting
   bubble still shows stored state. The panel now says so; it is not yet able to
   show otherwise.

9. **F-V005 — the Verbatim panel can contradict itself on first paint.** Found in
   D5a's evidence run, NOT introduced by it (`warnings()`, `live()`, `render()`
   and the fetch sequencing are byte-unchanged from D4). One shot caught the
   panel showing *No consultation fee — the most-asked price on any call* directly
   beneath a FACTS row reading *Consultation ₹500*.
   **Mechanism:** `warnings()` reads ONLY the live DOM (`verbatim.js:370`) while
   the FACTS row falls back to the SAVED value (`verbatim.js:253`). The panel
   renders the moment its own `/knowledge-summary` fetch resolves, which races the
   page's independent `/config/pricing` fetch. When the panel wins, it computes
   warnings against a form that has not been filled — and nothing re-renders it
   afterwards, because `fill()` sets `.value` programmatically and that fires no
   `input` event. The stale warning then survives until the owner types.
   ⚠️ **Frequency not established.** Observed once; a scripted probe mirroring the
   shot's exact conditions (2× device scale, panel expanded, both tenants) came
   back 0/12. It is a real race with a clear mechanism and an unknown rate, not a
   reliably reproducible defect. The probe is retained in `shootD5a.js` as a
   reported diagnostic rather than an assertion, so a future session inherits the
   measurement rather than the anecdote.
   **Fix when scheduled:** have the page signal fill completion, or have
   `warnings()` fall back to the saved value the way the FACTS row already does.
   Out of D5a's scope (Verbatim panel beyond W6).
7. **F-V003 — `readinessOnce()` swallows 401 to null.** Non-Home pages leave the header blank on a dead session instead of redirecting. `shell.js:592-600`. Error ladder layer 4. Found in D3 Phase 0, not fixed there — behaviour change, D3 is presentation-only.
8. **TEST-FLAKE-03 — CLOSED.** Fixed by seeding a `tenant_configs` row with seven-day open hours in the fixture, so the subtest controls its own preconditions. `clinicDefaults` is unchanged. Original report: **`voiceCancellation.integration.test.js:270` failed on any day when today+2 is a Sunday.** `istDateString(2)` picks the booking date; the fixture seeds Dr. Rao for all seven days but seeds **no `tenant_configs` row**, so clinic hours fall back to `clinicDefaults`, which closes Sunday. `book_appointment` refuses with `closed_day`, no row is written, and `assert.equal(appts.length, 1, 'the committed booking stands')` fails. Reproduced at `95d0f5f` with a clean tree, so it is not D3's. It passed in D3's own Phase 0 baseline the previous day (today+2 was a Saturday) and has failed every run since — which is exactly the signature. Fix is one of: seed the fixture's hours explicitly, or advance the date to the next open day. Not a D3 task; D3's only permitted test edit was the `shell.js` label assertion.

---

## 7. Sequencing note

D1 and D3 are correctness work: D1 closes the portal half of a filed finding and repairs a typography declaration that is currently false; D3 closes F-F001's portal half. Both are defensible against G-PROOF on their own terms.

D2, D4 and D5 are the redesign proper and remain H5. They are cheap, they are approved, and D4 is the one that would make a clinic owner in a demo say *it speaks Telugu* rather than *it looks like software*. That value is realised only in front of a prospect, and a prospect needs a working number.

**Run D1 and D3 now. Run D2, D4, D5 in the gap while Plivo KYC is pending — not instead of filing it.** The order that maximises the chance this work matters is unchanged from the spec: file the KYC, then build.
